# Streamlit para la interfaz web
streamlit

# OpenAI SDK
openai

# Tokenización (usado internamente por embeddings OpenAI)
tiktoken

# LangChain actualizada (core)
langchain>=0.1.0

# Nuevas versiones separadas de langchain (post 0.1.0)
langchain-community
langchain-openai

# Vector store FAISS (más simple para uso en la nube)
faiss-cpu

# Versión compatible de protobuf (evita errores de serialización)
protobuf==3.20.3